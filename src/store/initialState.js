import LINES_COUNT from '../constants/linesCount';

export default {
    config          : null,
    slotState       : null,
    spinInProgress  : false,
    lineBet         : null,
    linesCount      : LINES_COUNT.max,
};