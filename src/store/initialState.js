import { LINES_COUNT } from '../utils/constants';

export default {
    config          : null,
    slotState       : null,
    spinInProgress  : false,
    lineBet         : null,
    linesCount      : LINES_COUNT.max,
};