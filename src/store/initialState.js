import {
    LINE_BET,
    LINES_COUNT
} from '../utils/constants';

export default {
    config          : null,
    slotState       : null,
    spinInProgress  : false,
    lineBet         : LINE_BET.max,
    linesCount      : LINES_COUNT.max,
};